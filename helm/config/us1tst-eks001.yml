# Staging US — 2 replicas for PDB / rolling updates.

replicaCount: 2

image:
  pullPolicy: Always

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

existingSecret: sinch-mcp-server
