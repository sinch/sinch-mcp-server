# Production BR — 2 replicas; session state is shared via Redis (REDIS_URL), so
# requests can land on either pod.

replicaCount: 2

image:
  pullPolicy: IfNotPresent

resources:
  requests:
    cpu: 200m
    memory: 512Mi
  limits:
    cpu: 1000m
    memory: 1Gi

existingSecret: sinch-mcp-server
